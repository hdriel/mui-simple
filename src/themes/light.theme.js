import { createTheme } from "@mui/material/styles";

export default createTheme({
  direction: "rtl",
  palette: {
    primary: {
      main: "#00F100",
      contrastText: "#000000",
    },
    background: {
      hadriel: "#F0F0F0",
      dialog: "rgba(79,145,253,0.25)",
    },
  },
  spacing: 8,
});
