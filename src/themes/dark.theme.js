import { createTheme } from "@mui/material/styles";

export default createTheme({
  direction: "ltr",
  palette: {
    primary: {
      main: "#00f1f1",
      contrastText: "#000000",
    },
    background: {
      hadriel: "#010101",
      dialog: "rgba(178,226,68,0.25)",
    },
  },
  spacing: 8,
});
